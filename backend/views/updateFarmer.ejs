<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit</title>
  <style>
    /* Main container styling */
body {
  font-family: Arial, sans-serif;
  margin: 20px;
  background-color: #f9f9f9;
  color: #333;
}

/* Header styling */
h1 {
  color: #4CAF50;
  font-size: 2em;
  margin-bottom: 20px;
}

/* Form styling */
form {
  background-color: #fff;
  border: 1px solid #ddd;
  border-radius: 4px;
  padding: 20px;
  box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
}

form div {
  margin-bottom: 15px;
}

label {
  display: block;
  color: #333;
  font-size: 1em;
  margin-bottom: 5px;
}

input[type="text"], input[type="number"], input[type="file"], input[type="email"], input[type="password"], textarea, input[type="date"] {
  width: 100%;
  padding: 10px;
  border: 1px solid #ddd;
  border-radius: 4px;
  box-sizing: border-box;
}

textarea {
  resize: vertical;
  height: 100px;
}

/* Radio buttons styling */
input[type="radio"] {
  float: left;
  width: auto;
  margin-right: 5px;
}

/* Button styling */
button {
  background-color: #4CAF50;
  color: white;
  padding: 10px 20px;
  margin: 5px 2px;
  border: none;
  border-radius: 4px;
  cursor: pointer;
  font-size: 1em;
}

button:hover {
  background-color: #45a049;
}

/* Link styling */
a {
  color: #4CAF50;
  text-decoration: none;
  font-size: 1em;
}

a:hover {
  text-decoration: underline;
}

  </style>
</head>
<body>
  <h1>Edit</h1>
  <form action="/farmer/update" method="POST" enctype="multipart/form-data">
    <div>
      <label for="firstname">firstname</label>
      <input type="text" id="firstname" name="firstname" value="<%=farmer.firstname %>" required>
    </div>
    <div>
      <label for="lastname">lastname</label>
      <input type="text" id="lastname" name="lastname" value="<%=farmer.lastname %>" required>
    </div>

    <label for="gender">Gender:</label>
    <div id="gender">
      <input type="radio" id="female" name="gender" value="female" <% if(farmer.gender == 'female'){ %> <%= 'checked' %> <% } %>>
      <label for="female">Female</label>
      <input type="radio" id="male" name="gender" value="male" <% if(farmer.gender == 'male'){ %> <%= 'checked' %> <% } %>>
      <label for="male">Male</label>
    </div>
    <div>
      <label for="dob">Date of birth</label>
      <input type="date" id="dob" name="dob" value="<%=farmer.dob.toISOString().split('T')[0] %>" required>
    </div>
    <div>
      <label for="mobile">mobile</label>
      <input type="number" id="mobile" name="mobile" value="<%=farmer.mobile %>" required>
    </div>
    <div>
      <label for="address">address</label>
      <textarea id="address" name="address" required><%=farmer.address %></textarea>
    </div>
    <div>
      <label for="nid_img">nid_img</label>
      <input type="file" id="nid_img" name="nid" />
    </div>
    <div>
      <label for="pfp_img">pfp_img</label>
      <input type="file" id="pfp_img" name="pfp" />
    </div>
    <div>
      <label for="email">email</label>
      <input type="email" id="email" name="email" value="<%=farmer.email %>" required>
    </div>
    <div>
      <label for="password">password</label>
      <input type="password" id="password" name="password">
    </div>
    <button type="submit">Done</button>
  </form>

  <form action="/farmer" method="GET">
      <button type="submit">Cancel</button>
  </form>
</body>
</html>
