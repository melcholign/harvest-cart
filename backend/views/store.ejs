<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= store.storeName %></title>
  <style>
    /* Main container styling */
body {
  font-family: Arial, sans-serif;
  margin: 20px;
  background-color: #f9f9f9;
  color: #333;
}

/* Header styling */
h1 {
  color: #4CAF50;
  font-size: 2em;
  margin-bottom: 10px;
}

h2 {
  color: #FF5733;
  font-size: 1.5em;
  margin-bottom: 20px;
}

/* Status styling */
h3 {
  color: #333;
  font-size: 1.2em;
  margin-top: 10px;
}

h3.status-open {
  color: #4CAF50;
}

h3.status-closed {
  color: #FF5733;
}

/* Image styling */
img {
  border: 2px solid #ddd;
  border-radius: 4px;
  padding: 5px;
  width: 200px;
  height: 125px;
  margin-top: 10px;
}

/* Button styling */
button {
  background-color: #4CAF50;
  color: white;
  padding: 10px 20px;
  margin: 5px 2px;
  border: none;
  border-radius: 4px;
  cursor: pointer;
  font-size: 1em;
}

button:hover {
  background-color: #45a049;
}

/* Store and product containers styling */
div {
  margin-bottom: 20px;
}

div.product-container {
  border: 1px solid #ddd;
  border-radius: 4px;
  padding: 10px;
  margin-bottom: 10px;
  background-color: #fff;
}

form img {
  margin: 10px 0;
}

/* Link styling */
a {
  color: #4CAF50;
  text-decoration: none;
  font-size: 1em;
}

a:hover {
  text-decoration: underline;
}

  </style>
</head>
<body>
  <h1><%= store.storeName %></h1>
  <h2><%= store.description %></h2>

  <% if(store.isOpen){ %>
      <h3 class="status-open">Status: Open</h3>
  <% } else { %>
      <h3 class="status-closed">Status: Closed</h3>
  <% } %>

  <h3>Current rating: <%= store.rating %></h3>
  <h3>Cover Image:</h3>
  <img src="/<%= store.coverImgPath %>" alt="cover_img" width="200" height="125">
  <h3>Gallery Images:</h3>
  <% for(let i = 0; i < 8; i++){ %>
    <img src="/<%= store.galleryImgsPath %>/<%= i %>.jpg" width="200" height="125">
  <% } %>

  <form action="/farmer/store/<%= store.storeId %>/update" method="GET">
    <button type="submit">Edit Store</button>
  </form>

  <form action="/farmer/store/<%= store.storeId %>/delete" method="POST">
    <button type="submit">Delete Store</button>
  </form>

  <form action="/farmer/store/<%= store.storeId %>/product/add" method="GET">
    <button type="submit">Add Product</button>
  </form>

  <% for(let i = 0; i < products.length; i++){ %>
    <div class="product-container">
      <h3><%= i %>. product name:<%= products[i].productName %> ------- Rating:<%= products[i].rating %> ------ In stock:<%= products[i].stockQuantity %> ------ Price:<%= products[i].price %></h3>
      <img src="/<%= products[i].thumbnailImgPath %>" alt="product_thumbnail" width="200" height="125">
      <form action="/farmer/store/<%= store.storeId%>/product/<%= products[i].productId %>/update" method="GET">
        <button type="submit">Edit</button>
      </form>
      <form action="/farmer/store/<%= store.storeId%>/product/<%= products[i].productId %>/delete" method="POST">
        <button type="submit">Delete</button>
      </form>
    </div>
  <% } %>

  <a href="/farmer">Return to Dashboard</a>
</body>
</html>
